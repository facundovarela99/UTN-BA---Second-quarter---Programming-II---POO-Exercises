// 5. Create a cinema class that has private attributes movie and schedule.
    // ● Create public methods to the assignation and recover of the values
    // ● Create an instance and assigne values to it
    // ● Display the values
    // ● Create a second instance and assigne values to it
    // ● Display the values
    // ● Change the first instance's values
    // ● Display both instance values; conclude that instruction 'new' create different objects



